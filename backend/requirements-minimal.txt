# FastAPI and core dependencies (Python 3.13 compatible)
fastapi
uvicorn[standard]
python-multipart

# Database (use SQLite for simplicity with Python 3.13)
sqlalchemy
alembic

# Authentication and security
python-jose[cryptography]
passlib[bcrypt]
python-decouple

# Pydantic and validation
pydantic
pydantic-settings

# HTTP client for external APIs
httpx

# Environment and configuration
python-dotenv
